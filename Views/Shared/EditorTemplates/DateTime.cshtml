@model DateTime?
@*@Html.TextBox("", (Model.HasValue ? Model.Value.ToString("yyyy/MM/dd") : ""), new { @class = "datebox" })*@
@Html.TextBox("", (Model.HasValue ? Model.Value.ToString("yyyy-MM-dd") : ""), new { type = "date" })
